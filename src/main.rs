extern crate prokaryote;

use prokaryote::run;

fn main() {
    run();

    // let a: char = char::from(0u8);
    // println!("{}", a);

    // let mut a_buf = [0; 1];
    // let mut t_buf = [0; 1];
    // let mut buf_c = [0; 1];
    // let mut g_buf = [0; 1];
    // {
    // 	'A'.encode_utf8(&mut a_buf);
    // 	'T'.encode_utf8(&mut t_buf);
    // 	'C'.encode_utf8(&mut buf_c);
    // 	'G'.encode_utf8(&mut g_buf);
    // }
    // println!("{}", a_buf[0]);
    // println!("{}", t_buf[0]);
    // println!("{}", buf_c[0]);
    // println!("{}", g_buf[0]);
}
